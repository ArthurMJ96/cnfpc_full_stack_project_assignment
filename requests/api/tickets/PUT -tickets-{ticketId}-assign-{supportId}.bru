meta {
  name: PUT /tickets/{ticketId}/assign/{supportId}
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/tickets/:ticketId/assign/:supportId
  body: none
  auth: inherit
}

params:path {
  ticketId: 4
  supportId: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
